.user-role.btn-group
  - Role.active.each do |role|
    = link_to role.value, '#', class: "btn role-chose-button #{'active' if role.default_chosen}",
      remote: true, data: {percent_id: "#{role.percents.last.id if role.percents.exists?}", role_id: role.id}